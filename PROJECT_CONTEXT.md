# 项目共享上下文

本文件是用户、Codex、Claude 以及任何其他协作助手的共同交接文档。修改代码前先阅读，
在架构、构建流程、验证结果或已知问题发生实质变化后及时更新。

本项目尚处于规划阶段，还没有写任何 RTL。下面的数字来自 2026-08-12 的可行性讨论，
标注为「待确认」的条目在动手实现前需要先核实。

## 项目目的

用 Digilent Basys 3（`XC7A35T-1CPG236C`）做一台逻辑分析仪，主要用途是抓取并解码
STM32 开发板上的 SPI 通信。

这是 `Entrega_Duan`（github.com/hongxuanD/FPGA-RS232）复活完成后的后继项目。
那个项目的 RS-232 收发器、Tcl 工程流程和文档习惯可以直接借鉴。

## 硬件

- FPGA：Basys 3 上的 `XC7A35T-1CPG236C`
- 系统时钟：板载 100 MHz 晶振
- 探针接口：Pmod `JA` / `JB` / `JC`，每口 8 个信号引脚，另有 `JXADC`
- 电平：**3.3V LVCMOS，不耐 5V**。STM32 同为 3.3V，可直连
- 上位机链路：板载 FT2232HQ，同一根 micro-USB 同时提供 JTAG 和 USB-UART
- 板上可用资源：16 个拨码开关、16 个 LED、5 个按键、4 位数码管
- 用户已有：探针、杜邦线、STM32 开发板（作为被测对象）

**板上没有外部 RAM。** 采样深度完全受限于 BRAM，这是本项目最重要的约束。

## 目标信号：SPI

需要 4 个通道：`SCK`、`MOSI`、`MISO`、`CS`。设计按 8 通道做，余下 4 路留给
第二个片选，或 STM32 侧手工翻转的调试标志引脚。

采样率与 SCK 频率的关系（系统采样率 100 MSa/s）：

| SCK | 过采样倍数 | 评价 |
| --- | --- | --- |
| 1 MHz | 100x | 轻松 |
| 10 MHz | 10x | 推荐上限 |
| 25 MHz | 4x | 勉强，边沿判定开始不稳 |
| 50 MHz | 2x | 不可用 |

**首个验证目标（STM32）的 SCK 最高约 8 MHz。** 这落在舒适区内，对设计有几点实际影响：

| 量 | 值 |
| --- | --- |
| SCK 周期 | 125 ns |
| 每周期采样点（100 MSa/s） | 12.5 |
| 每字节（8 clk） | 1 us = 100 个采样点 |
| 2 ms 缓冲可抓 | 约 2000 字节 |

- 12.5 倍过采样足够干净，边沿判定不会飘。
- 距杜邦线的 10–30 MHz 极限还有约三倍余量，信号完整性大概率不构成问题。
- **不需要 ISERDES 或任何高速输入技巧**，普通 IOB 寄存器加双触发器同步即可。
- 采样分频器可以推迟到阶段 3，阶段 1 直接固定 100 MSa/s。

仍建议**先从 1 MHz 跑通再逐步加速到 8 MHz**，顺便测出这套杜邦线的实际极限。
该极限值应记录进本文件。

### 后续可选：状态模式采集

商用逻辑分析仪区分时序模式（固定采样率）和状态模式（以被测时钟为采样时钟）。
对 SPI 这类同步协议，状态模式每个数据位只存一个样本，而不是 12.5 个：

| 模式 | 采样时机 | 8 MHz SPI 下的深度 |
| --- | --- | --- |
| 时序模式 | 固定 100 MSa/s | 约 2000 字节 |
| 状态模式 | 仅在 SCK 有效沿 | 约 25000 字节 |

代价是丢失时序信息——看不到毛刺，量不了建立/保持时间，也测不出 SCK 实际频率。
它是时序模式的补充而非替代。若今后需要抓取整段几千字节的传输，这是唯一可行的路，
但不应在阶段 1–2 引入。

最有用的触发条件是 **`CS` 下降沿**。没有它，抓到的只是「按键之后的 2 ms」；
有了它才是「这次传输」。优先级高于其他所有触发类型。

## 三个硬约束

### 一、采样深度

`XC7A35T` 有 50 个 36Kb BRAM，合计 1800 Kb = **225 KB**。扣除开销按 200 KB 估算：

| 通道数 | 每样本 | 可存样本数 | @100 MSa/s 时长 |
| --- | --- | --- | --- |
| 8 | 1 字节 | ~200K | **2.0 ms** |
| 16 | 2 字节 | ~100K | 1.0 ms |
| 32 | 4 字节 | ~50K | 0.5 ms |

2 ms 对协议分析够用：10 MHz SCK 下一个字节 800 ns，可抓约 2500 字节；
1 MHz 下可抓约 250 字节。**但这要求触发可用**，否则抓不到想看的那一段。

后续若需要更长时间跨度，可考虑游程编码（RLE）——SPI 空闲期长，压缩比会很可观。

### 二、上传带宽

115200 baud 只有 11.5 KB/s，传 200 KB 需要 **17 秒**。每抓一次等 17 秒不可接受。

**把 UART 提到 921600 或 1 Mbaud 是必做项，不是优化。** 提速后约 2 秒。

### 三、信号完整性

杜邦线无屏蔽、无每信号地回流，实际可靠捕获的边沿速率估计在 **10–30 MHz**。
这和 SCK 的推荐上限正好撞在一起。

**接线要求：地线接多根，不是一根。** Pmod 每口有 2 个 GND 引脚，两个都接。

## 平台硬限制

以下是 Basys 3 无法通过设计规避的能力边界，属于事实而非风险。
列出来是为了在目标变化时能立刻判断「这块板子还够不够」。

| 限制 | 数值 | 成因 | 是否影响当前需求 |
| --- | --- | --- | --- |
| 采集深度上限 | 约 225 KB（8 通道约 200K 样本，100 MSa/s 下约 2 ms） | 板上无外部 RAM，只有 50 个 36Kb BRAM | **否**。单次 SPI 传输仅几百微秒 |
| 无法全速流式采集 | 主机链路上限约 1.2 MB/s，而 8 通道 100 MSa/s 是 100 MB/s | 板载 FT2232HQ 走 UART，最高约 12 Mbaud；板上无 USB 3.0、无以太网 | **否**。当前需求是触发后的单次快照 |
| 输入门限固定 | 约 1.65 V，不可调，无迟滞配置 | Pmod 的 VCCO 固定为 3.3 V，用的是 FPGA 的 LVCMOS 输入 | **否**。目标设备均为 3.3 V |
| 不耐 5 V | — | 同上 | **否**。同上 |

三点补充说明：

- **RLE 压缩不改变深度上限。** 它对空闲期长的信号（如 SPI）能把有效时长拉长几十倍，
  但对稠密数据无效，原始样本数的天花板不变。
- **低速下仍可流式。** 1.2 MB/s 除以 8 通道即约 1.2 MSa/s，若采样率降到 1 MSa/s
  以下，理论上可以边采边传。这对慢速协议有意义，对 8 MHz SPI 无意义。
- **采样率不是硬限制。** 用 PLL 倍频配合 `ISERDESE2` 过采样，理论上可以做到
  400 MSa/s 以上。实际瓶颈在 Pmod 通路和探针，不在 FPGA。因此不列入本表。

## 风险表

与硬限制不同，以下是尚未核实的事项。按**失败后果的严重程度**排序，
而不是按发生概率——排在前面的两条共同特征是**失败时会给出看似合理的错误数据**，
比明显的失败更危险。

| # | 风险 | 影响 | 后果 | 缓解 | 确认时机 |
| --- | --- | --- | --- | --- | --- |
| 1 | 杜邦线信号完整性不足 | `SYS-002` 的有效性 | 边沿振铃被判成多次跳变，解码出**看似合理的错误字节** | 先用零成本手段：两个 GND 都接、信号与地双绞、闲置引脚驱动低电平作伪地。不足时可购置带地线的测试夹或屏蔽排线——`STK-004` 允许购置配件 | 阶段 1，用已知内容的 SPI 对照 |
| 2 | UART 无流控导致静默丢字节 | `SYS-006` `SYS-007` | 丢一个字节后全部错位，**同样是看似合理的垃圾** | 改为握手式上传；上传帧加序号与校验 | 阶段 1 |
| 3 | 主机端不支持非标波特率 | `SYS-007` | 退回低波特率，慢但数据正确 | 1 Mbaud 是 100 MHz 的整数分频；实在不行用 460800 仍满足 3 秒 | 阶段 1 |
| 4 | 被测板上控制线无可用测试点 | `STK-002` | 只能抓 SPI 四线，看不到中断时序 | 无法从 FPGA 侧解决，取决于被测硬件 | 实际接线时 |
| 5 | BRAM 实际可用比例低于估计 | `SYS-003` | 深度缩水 | 下调深度，或提前引入 RLE | 综合后看资源报告 |

### 关于风险 2 的展开

上传链路有四环：**FPGA 分频器 → FT2232HQ → USB → 主机驱动与程序**。
风险分布在不同环节，性质不同：

**分频精度**（FPGA 侧）。100 MHz 除以目标波特率：

| 波特率 | 分频比 | 误差 |
| --- | --- | --- |
| 115200 | 868.06 | 0.007% |
| 921600 | 108.5 | 0.46% |
| **1000000** | **100** | **0** |
| 2000000 | 50 | 0 |

UART 容限一般为 2–3%，921600 也能用，但 **1 Mbaud 是整数分频，没有理由不选它**。
这一环不构成风险，是设计选择。

**流控缺失**（真正的风险）。FT2232HQ 的接收缓冲只有几 KB 量级，2 Mbaud 下
每秒进 200 KB。若主机来不及取走，缓冲溢出即丢字节，而裸 8N1 **没有任何流控**，
FPGA 不会知道。Basys 3 是否引出了 RTS/CTS 需查 Digilent 的 master XDC 确认——
`FPGA-RS232` 项目的约束文件里只有 `B18` / `A18` 两个引脚，但那只说明当时只用了两根。

因此确定两条设计决定：

1. **上传采用握手式**，由主机请求一块、FPGA 发一块，天然不会溢出。
   SUMP 协议本身就是这个模式，与阶段 4 的方向一致。
2. **上传帧携带序号与校验**，把静默丢失变成明确失败。成本几乎为零。

## 架构

```text
探针（Pmod JA/JB/JC，3.3V）
    |
    v
双触发器同步器 x N ------ 跨时钟域，防亚稳态
    |
    v
采样分频器 -------------- 100 MHz / (1,2,5,10,...)
    |
    v
触发单元 ---------------- 值 + 掩码匹配；SPI 场景优先做 CS 下降沿
    |
    v
BRAM 环形缓冲 ----------- 触发前 / 触发后比例可调
    |
    v
UART 上传 --------------- 921600 或更高
    |
    v
上位机 ------------------ Python -> VCD -> GTKWave；后期改为 SUMP -> PulseView
```

板上现成外设的用法：拨码开关配置触发条件，LED 显示 armed / triggered / full
状态，数码管显示采样率或已捕获样本数。

## 分阶段计划

**阶段 1：证明数据通路能走通**

8 通道，按键触发，固定 100 MSa/s，抓满即停，十六进制吐给串口，Python 打印。
不做触发器、不做分频、不做环形缓冲。目标是看到真实的波形数据。

**阶段 2：变成真正的仪器**

环形缓冲 + 触发 + 触发前采样。首先实现 `CS` 下降沿触发。

**阶段 3：可配置**

采样率分频、深度、通道数，以及一套像样的命令协议。

**阶段 4：接入 PulseView**

实现 **SUMP 协议**（OpenBench Logic Sniffer）。sigrok 有现成的 `ols` 驱动，
实现之后 PulseView 直接可用，附带几十种协议解码器（含 SPI）。

自己写 VCD 导出只是阶段 1–2 的过渡方案；SUMP 才是终点，能省掉大量上位机工作。

## 从 Entrega_Duan 可复用的部分

**UART 收发器**：`RS232_RX.vhd` 和 `TX.vhd` 可以借鉴结构，但**必须重写**。
原实现把 `Pulse_width` 写死为 `20000000/115200`，本项目跑 100 MHz 且波特率更高，
应改为 generic 参数化。原项目「把 UART 时钟频率参数化」这条 TODO 正好在此兑现。

注意原 `RS232_RX` 在 `StopBit` 状态下 `Store_out` 会保持高电平，导致每个字节被
写入 FIFO 两次；原设计靠 DMA「排空取最后值」来消解。**重写时不要照搬这个行为**，
新的接收器应当每字节只产生一个写脉冲。

**Tcl 工程流程**：`create_project.tcl` / `verify_project.tcl` 的结构直接照搬。
原项目吃过 `.xpr` 和 IP 文件丢失的亏，本项目从第一天起就保持工程可脚本重建，
不把 Vivado 生成物纳入版本管理。

**文档习惯**：本文件即是。

## 待确认的事实

动手前需要核实，不要当成已知条件：

1. Basys 3 Pmod 口的串联保护电阻阻值（估计 200 欧，需查 reference manual）。
   它与线缆电容构成低通，会进一步限制可捕获的边沿速率。
2. 板载 FT2232HQ 在本机实际可用的最高 UART 波特率。921600 应当可行，更高需实测。
3. `XC7A35T` 的 BRAM 在实际布线后可用于采样缓冲的比例（上文按 200/225 估算）。
4. 杜邦线在本套装配下的实际边沿速率上限——通过逐步提高 STM32 的 SPI 时钟测得。
5. ~~SR150 与主机 MCU 之间实际使用的 SPI 时钟频率。~~
   **2026-08-12 已核实：8.000 MHz，与首个验证目标一致，风险解除。** 详见下方
   「已核实：QN9090 与 SR150 的 SPI 配置」。

## 已核实：QN9090 与 SR150 的 SPI 配置

2026-08-12 查阅生产固件仓库 `Nolo-Firmware/mk-nolo-firmware` 得到，
来源为 `boards/mk_shield/driver_config.h` 和 `boards/mk_shield/uwb_bus_interface.c`。

| 参数 | 值 | 对分析仪的意义 |
| --- | --- | --- |
| SCK | **8.000 MHz** | 源时钟 FRO 32 MHz 四分频，精确值而非近似。100 MSa/s 下 12.5 倍过采样 |
| CPOL / CPHA | 0 / 0（Mode 0） | PulseView 的 SPI 解码器需按此配置 |
| 数据宽度 | 8 位 | |
| 位序 | MSB first | |
| 片选极性 | 低有效 | 触发条件用下降沿，与设计一致 |
| 传输方式 | DMA，帧间由 `delayConfig` 插入延时 | 传输不是完全背靠背 |

固件中的注释写明 8 MHz 是 **QN9090 的 SPI 上限**，而非 SR150 的限制。
瓶颈在主机侧。

### 实际的信号清单

`boards/mk_shield/pin_mux.c` 中 SPI1 只路由了四个引脚，**总线上只有 SR150
一个从设备**：

| 板上标号 | QN9090 引脚 | 信号 |
| --- | --- | --- |
| `SCK` | PIO0_0 | SPI 时钟 |
| `SDI` | PIO0_2 | MOSI |
| `SDO` | PIO0_1 | MISO |
| `CS_N` | PIO0_3 | 片选（SSELN0），低有效 |

`driver_config.h` 中另有一行 `#define ACCEL_SPI_SSEL kSPI_Ssel1`，
但该宏在整个代码库中**只有定义、没有任何使用**，`pin_mux.c` 也未路由第二路片选。
按现有固件判断它是死代码，不构成额外的采集需求。

除 SPI 四线外，`uwb_bus_io.c` 还操作 SR150 的几根控制线
（NXP 内部代号 Helios），抓上它们能看出交互的完整节奏：

| 信号 | 作用 | 价值 |
| --- | --- | --- |
| `HELIOS_GPIO5_HOST_INT` | SR150 到主机的中断 | **最有价值**。能看出主机为何在某个时刻发起传输 |
| `HELIOS_CE` | 芯片使能 | 观察上电与复位时序 |
| `HELIOS_GPIO3_SYNC` | 同步信号 | 固件中有引用 |

因此合理的分配是 **4 路必需 + 2 至 3 路控制线 = 6 至 7 路**，
`SYS-001` 要求的 8 通道留有 1 至 2 路余量。

⚠️ **尚未核实**：上述控制线在实际板子上是否有可供探针接入的测试点。
若无法接入，可采集的就只有 SPI 四线。

**残留的不确定**：8 MHz 是 QN9090 的能力上限。若今后主机换成基于 nRF52840 的
Raytac 模组，其 SPIM3 可达 32 MHz，届时需重新确认。

## 协作规则

沿用 `Entrega_Duan` 的约定：

1. 编辑前先阅读本文件，并执行 `git status --short --branch` 和
   `git log -5 --oneline --decorate`。
2. 保留用户或另一个助手未提交的工作，绝不使用破坏性的 reset / checkout。
3. **提交必须经过用户明确许可，每次单独确认，授权不延续到下一次。**
   获准后直接提交到 `main`，不开分支。提交信息只写正文，**不加任何署名 trailer**。
4. 推送到 GitHub 是独立的一步，需要单独许可。
5. 运行 Vivado 仿真前先询问；用户希望自己在 GUI 里观察波形。
   需要仿真证据时，给出具体的 GUI 操作步骤和判据，由用户执行。
6. 完成实质性工作后更新本文件的工作日志，记录事实和命令，不写推测性结论。

## 工作日志

| 日期 | 贡献者 | 工作 |
| --- | --- | --- |
| 2026-08-12 | 用户 + Claude | 完成可行性讨论，确定目标信号为 SPI，建立仓库骨架与本文件。尚未编写任何 RTL。 |
| 2026-08-12 | 用户 | 确认目标 SCK 最高约 8 MHz。据此确定阶段 1 固定 100 MSa/s、无需高速输入技巧，并记录状态模式作为后续可选方向。 |

## 写 SYS.3 时要落实的可替换点

架构文档应逐个指名可替换的位置，并写明各自的动机。**没有已识别动机的地方不要抽象**，
为想象中的未来做接口通常是浪费，还会让代码变复杂。目前已识别三处：

| 可替换点 | 动机 |
| --- | --- |
| 存储后端 | BRAM 到 DDR，若今后更换开发板。见「平台硬限制」 |
| 上传协议 | 自定义协议与 SUMP 之间切换，见 `docs/sys2/` 中 `SYS-014` 的派生说明 |
| 触发判定逻辑 | 组合匹配到串行模式匹配。**这是唯一与协议相关的部分**，其余采集通路协议无关 |

## 下一步

1. 核实上文「待确认的事实」中的 1 和 2。
2. 确定 Pmod 引脚分配，编写 `constraints/` 下的 XDC。
3. 编写参数化的 UART 发送器，先做到能从板子吐出固定测试数据。
4. 按阶段 1 实现最小采样通路。
