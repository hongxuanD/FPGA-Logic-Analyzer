# 项目共享上下文

本文件是用户、Codex、Claude 以及任何其他协作助手的共同交接文档。

它的定位是**入口、共享事实与协作约定**。需求、架构、计划和风险等过程产物
都在 `docs/` 下，本文件只做导航，**不复制其内容**——一旦为了「方便阅读」
把别处的结论摘一份过来，两边立刻开始漂移。

本项目尚处于设计阶段，还没有写任何 RTL。

## 项目目的

用 Digilent Basys 3（`XC7A35T-1CPG236C`）做一台逻辑分析仪，主要用途是抓取并解码
STM32 与 QN9090／SR150 等设备上的 SPI 通信。

这是 `Entrega_Duan`（github.com/hongxuanD/FPGA-RS232）复活完成后的后继项目。

## 文档导航

| 想知道什么 | 看哪里 |
| --- | --- |
| 为什么做、要做成什么样 | `docs/sys1/` 干系人需求 |
| 具体指标与验证方法 | `docs/sys2/` 系统需求 |
| 分成哪几块、接口是什么 | `docs/sys3/` 系统架构 |
| 什么时候做什么 | `docs/man3/` 生命周期与阶段计划 |
| 还有哪些不确定 | `docs/man5/` 风险登记册 |
| 文档体系本身 | `docs/README.md` |

## 硬件

- FPGA：Basys 3 上的 `XC7A35T-1CPG236C`
- 系统时钟：板载 100 MHz 晶振
- 探针接口：Pmod `JA` / `JB` / `JC`，每口 8 个信号引脚，另有 `JXADC`
- 电平：**3.3V LVCMOS，不耐 5V**。目标设备同为 3.3V，可直连
- 上位机链路：板载 FT2232HQ，同一根 micro-USB 同时提供 JTAG 和 USB-UART
- 板上可用资源：16 个拨码开关、16 个 LED、5 个按键、4 位数码管
- 用户已有：探针、杜邦线、STM32 开发板、Basys 3

板上另有一个 Type-A USB 口（`J2`），是由 PIC24FJ128 驱动的 **USB HID 主机口**，
供接键鼠或从 U 盘加载 bitstream。它是主机口不是设备口，**无法用于与电脑通信**。

### 平台硬限制

以下是 Basys 3 无法通过设计规避的能力边界，属于事实而非风险。
列出来是为了在目标变化时能立刻判断「这块板子还够不够」。

| 限制 | 数值 | 成因 | 是否影响当前需求 |
| --- | --- | --- | --- |
| 采集深度上限 | 约 225 KB（8 通道约 200K 样本，100 MSa/s 下约 2 ms） | 板上无外部 RAM，只有 50 个 36Kb BRAM | **否**。单次 SPI 传输仅几百微秒 |
| 无法全速流式采集 | 主机链路上限约 1.2 MB/s，而 8 通道 100 MSa/s 是 100 MB/s | FT2232HQ 走 UART 最高约 12 Mbaud；板上无 USB 3.0、无以太网 | **否**。当前需求是触发后的单次快照 |
| 输入门限固定、不耐 5V | 约 1.65 V，不可调，无迟滞配置 | Pmod 的 VCCO 固定为 3.3 V | **否**。目标设备均为 3.3 V |

三点补充：

- **游程编码不改变深度上限。** 它对空闲期长的信号能把有效时长拉长几十倍，
  但对稠密数据无效，原始样本数的天花板不变。
- **低速下仍可流式。** 1.2 MB/s 除以 8 通道即约 1.2 MSa/s，若采样率降到
  1 MSa/s 以下，理论上可以边采边传。这对慢速协议有意义，对 8 MHz SPI 无意义。
- **采样率不是硬限制。** 用 PLL 倍频配合 `ISERDESE2` 过采样理论上可达
  400 MSa/s 以上，实际瓶颈在 Pmod 通路和探针，不在 FPGA。

## 已核实：QN9090 与 SR150 的 SPI 配置

2026-08-12 查阅生产固件仓库 `Nolo-Firmware/mk-nolo-firmware` 得到，
来源为 `boards/mk_shield/driver_config.h`、`uwb_bus_interface.c` 和 `pin_mux.c`。

| 参数 | 值 | 对分析仪的意义 |
| --- | --- | --- |
| SCK | **8.000 MHz** | 源时钟 FRO 32 MHz 四分频，精确值而非近似。100 MSa/s 下 12.5 倍过采样 |
| CPOL / CPHA | 0 / 0（Mode 0） | PulseView 的 SPI 解码器需按此配置 |
| 数据宽度 | 8 位 | |
| 位序 | MSB first | |
| 片选极性 | 低有效 | 触发条件用下降沿，与设计一致 |
| 传输方式 | DMA，帧间由 `delayConfig` 插入延时 | 传输不是完全背靠背 |

固件注释写明 8 MHz 是 **QN9090 的 SPI 上限**，而非 SR150 的限制，瓶颈在主机侧。

### 实际的信号清单

`pin_mux.c` 中 SPI1 只路由了四个引脚，**总线上只有 SR150 一个从设备**：

| 板上标号 | QN9090 引脚 | 信号 |
| --- | --- | --- |
| `SCK` | PIO0_0 | SPI 时钟 |
| `SDI` | PIO0_2 | MOSI |
| `SDO` | PIO0_1 | MISO |
| `CS_N` | PIO0_3 | 片选（SSELN0），低有效 |

`driver_config.h` 中另有一行 `#define ACCEL_SPI_SSEL kSPI_Ssel1`，
但该宏在整个代码库中**只有定义、没有任何使用**，`pin_mux.c` 也未路由第二路片选。
按现有固件判断它是死代码。

除 SPI 四线外，`uwb_bus_io.c` 还操作 SR150 的几根控制线（NXP 内部代号 Helios），
抓上它们能看出交互的完整节奏：

| 信号 | 作用 | 价值 |
| --- | --- | --- |
| `HELIOS_GPIO5_HOST_INT` | SR150 到主机的中断 | **最有价值**。能看出主机为何在某个时刻发起传输 |
| `HELIOS_CE` | 芯片使能 | 观察上电与复位时序 |
| `HELIOS_GPIO3_SYNC` | 同步信号 | 固件中有引用 |

合计 **4 路必需 + 2 至 3 路控制线 = 6 至 7 路**，`SYS-001` 的 8 通道留有余量。
这些控制线在实物板上是否有测试点尚未核实，见 `docs/man5/` 的 R-003。

## 从 Entrega_Duan 可复用的部分

**UART 收发器**：`RS232_RX.vhd` 和 `TX.vhd` 可以借鉴结构，但**必须重写**。
原实现把 `Pulse_width` 写死为 `20000000/115200`，本项目跑 100 MHz 且波特率更高，
应改为 generic 参数化。原项目「把 UART 时钟频率参数化」这条 TODO 正好在此兑现。

⚠️ 注意原 `RS232_RX` 在 `StopBit` 状态下 `Store_out` 会保持高电平，导致每个字节被
写入 FIFO 两次；原设计靠 DMA「排空取最后值」来消解。**重写时不要照搬这个行为**，
新的接收器应当每字节只产生一个写脉冲。

**Tcl 工程流程**：`create_project.tcl` / `verify_project.tcl` 的结构直接照搬。
原项目吃过 `.xpr` 和 IP 文件丢失的亏，本项目从第一天起就保持工程可脚本重建，
不把 Vivado 生成物纳入版本管理。

## 协作规则

1. 编辑前先阅读本文件，并执行 `git status --short --branch` 和
   `git log -5 --oneline --decorate`。
2. 保留用户或另一个助手未提交的工作，绝不使用破坏性的 reset / checkout。
3. **提交必须经过用户明确许可，每次单独确认，授权不延续到下一次。**
   获准后直接提交到 `main`，不开分支。提交信息只写正文，**不加任何署名 trailer**。
4. 推送到 GitHub 是独立的一步，需要单独许可。
5. 运行 Vivado 仿真前先询问；用户希望自己在 GUI 里观察波形。
   需要仿真证据时，给出具体的 GUI 操作步骤和判据，由用户执行。
6. **把一条声明写进文档之前，先跑那个能证伪它的廉价检查**——一次 grep、
   一次查手册、一次仿真。区分哪些是观测到的、哪些是推断的，
   未核实的放进 `docs/man5/` 的风险登记册，不要当事实写。
7. 完成实质性工作后更新下方工作日志，记录事实和命令，不写推测性结论。

## 工作日志

| 日期 | 贡献者 | 工作 |
| --- | --- | --- |
| 2026-08-12 | 用户 + Claude | 完成可行性讨论，确定目标信号为 SPI，建立仓库骨架。尚未编写任何 RTL。 |
| 2026-08-12 | 用户 | 确认目标 SCK 最高约 8 MHz。据此确定阶段 1 固定 100 MSa/s、无需高速输入技巧。 |
| 2026-08-12 | 用户 + Claude | 查阅生产固件，核实 QN9090 与 SR150 的 SPI 配置与信号清单。 |
| 2026-08-12 | 用户 + Claude | 编写 SYS.1 与 SYS.2，按轻量 ASPICE 组织 `docs/`。评审推翻了多条初稿结论。 |
| 2026-08-12 | 用户 + Claude | 编写 SYS.3。核实 SUMP 支持 XON/XOFF 且波特率可选后，取消自研协议与自研上位机，系统元素由四个减为三个。重排阶段计划，SUMP 提前到阶段 1。拆出 MAN.3 与 MAN.5，精简本文件。 |

## 下一步

1. 编写 `docs/swe1/` 与 `docs/swe2/`，覆盖完整架构。
2. 在实现前核实 SUMP 的命令字列表与长度、metadata 格式、flags 对每样本字节数的
   影响，记入 SWE.2。见 `docs/man5/` 的 R-006。
3. 确定 Pmod 引脚分配，编写 `constraints/` 下的 XDC。
4. 按 `docs/man3/` 的阶段 1 实现，先做 1a 只应答 ID。
